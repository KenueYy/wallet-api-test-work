version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: backend
      POSTGRES_USER: user  
      POSTGRES_PASSWORD: pass
    ports:
      - "5432:5432"
